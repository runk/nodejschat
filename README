Instructions:

1. Run socket server from terminal using following command:
$ node server.js

2. Compile client and open it in flash player. Otherwise you can use pre-compiled one, in client/bin directory.

